{
  "name": "03-exercise-testing-dojo",
  "author": "Developers Institute",
  "version": "1.0.0",
  "scripts": {
    "test": "jest --verbose --coverage --coverageDirectory='coverage'",
    "test:watch": "jest --verbose --coverage --coverageDirectory='coverage' --watch",
    "start": "nodemon server.js"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "@types/jest": "^26.0.20",
    "express": "^4.17.1",
    "faker": "^5.5.3"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
